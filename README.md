# Rede Universitas
---
Lorem ipsum

## Como rodar o projeto?
Execute os seguintes comandos:
1. `python -m venv .venv`
2. `pip install -r requirements.txt` (instalação de bibliotecas)
3. `streamlit run app.py`